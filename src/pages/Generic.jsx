import React from 'react';import {Button,Eyebrow,List,SectionHead} from '../components/UI';
export default function Generic({title,eyebrow,copy,sections,image}){return <><section className="pagehero"><div className="container pagehero-grid"><div><Eyebrow>{eyebrow}</Eyebrow><h1>{title}</h1><p>{copy}</p><div className="actions"><Button to="/book-a-strategy-call">Book a Strategy Call</Button><Button to="/case-studies" variant="secondary">Explore Our Work</Button></div></div>{image&&<img src={image} alt={title}/>}</div></section><section className="section"><div className="container details">{sections.map((s,i)=><div className="detail" key={i}><span>0{i+1}</span><h3>{s.title}</h3><p>{s.copy}</p>{s.items&&<List items={s.items}/>}</div>)}</div></section></>}
